"""Token-usage time-series analysis modules.

All modules take pre-bucketed/densified inputs (lists, not Records) so they
have no dependency on token_stats.py and can be unit-tested independently.
"""
